{
  "name": "workshop-base",
  "version": "1.0.0",
  "description": "Universal OnlyWorlds tool foundation — workshop-ready base for any build",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@onlyworlds/sdk": "^2.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
